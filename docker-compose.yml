services:
  db:
    image: pgvector/pgvector:pg16
    environment:
      POSTGRES_USER: wayfind
      POSTGRES_PASSWORD: wayfind
      POSTGRES_DB: wayfind
    ports:
      - "5433:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data
volumes:
  pgdata:
